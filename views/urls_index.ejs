<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ../partials/_header %>
  </header>

  <ul>
    <% for (var url in urls) { %>
      <li>
        <%= url%>: <%= urls[url] %>
          <form method="POST" action=<%= '/urls/' + url + '/delete'%>>
            <input type='submit' value='DELETE' />
          </form>
      </li>
    <% } %>
  </ul>
  <a href="">Click here to shorten a URL!</a>
</body>
</html>