<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <header>
    <% include ../partials/_header %>
  </header>

  <% if(loggedAsEmail) { %>
  <form method="POST" action=<%= '/urls/' + shortURL + '/update'%>>
    <br>
    Input a full URL to change where [<%= shortURL%>] leads.
    <br>
      <input type='text' name='inputURL'/>
      <input type='submit' value='UPDATE'/>
  </form>
  <% } else { %>
  <br>
  You need to be logged in to edit URLs!
  <% } %>
</body>
</html>